interface Props {
    currentUserId: string;
    accountId: string;
    accountType: string;
}


const ThreadsTab = async function ({ currentUserId, accountId, accountType }: Props) {
    // TODO:Fetch profile threads

    return (
        <section>
            ThreadsTab
        </section>
    )
}

export default ThreadsTab;